# fsr

A filesystem router for Clojure web projects.

**fsr** maps HTTP request URIs to Clojure namespace files based on filesystem structure. Zero dependencies, supports dynamic apps and static sites.

See [docs/index.md](docs/index.md) for documentation and quick start.
